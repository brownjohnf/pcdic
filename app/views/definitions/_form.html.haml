= bootstrap_form_for @definition do |f|
  = f.collection_select :word_id, Word.all, :id, :first_form
  = f.text_field :content
  = f.radio_buttons :priority, { :low => 15, :medium => 10, :high => 5 }
  = f.submit 'Save'
