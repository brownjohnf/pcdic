.container
  .row
    .span3
      .well
        %h1= @language.name
        %p= @language.description
        %p= pluralize(@language.words.count, 'word')
    .span9
      = render @language.words.limit(10)
